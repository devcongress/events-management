# Technical Debt Register

This register is intentionally direct. It helps contributors understand what is prototype-grade, what is risky before production, and where useful contributions can land.

## Priority Guide

| Priority | Meaning |
|---|---|
| P0 | Must resolve before public production use |
| P1 | Should resolve before broad community adoption |
| P2 | Important maintainability or quality improvement |
| P3 | Nice-to-have cleanup |

## Current Debt

| Priority | Area | Debt | Evidence | Suggested Direction |
|---|---|---|---|---|
| P0 | Persistence | Hosted events, feedback, organizer auth, and the quiz runtime are relational in Supabase, but talks, speakers, attendance, checklists, users, volunteer applications, and other compatibility domains still rewrite whole arrays in `app_json_documents`; serialization is isolate-local and can lose concurrent Worker updates. Local development also retains filesystem fallback behavior. | `lib/mock-db/index.ts`; `supabase/migrations/20260630000000_app_json_documents.sql`; `supabase/migrations/20260801000000_quiz_participants.sql`; `supabase/migrations/20260801010000_relational_quiz_runtime.sql`; `docs/website-integration/supabase-foundation.md` | Move each remaining compatibility domain to constrained relational tables, backfill and reconcile it, switch to one writer, then retire its JSON document. |
| P1 | Quiz realtime | Quiz clients still poll every 1500ms. Phase changes now happen through explicit `POST /api/quiz/state/advance`, but there is still no realtime/job-backed state machine for larger live games. | `server/quiz-state.ts`; `server/app.ts`; `docs/features/quiz.md` | Move phase transitions to a server job, Durable Object, Supabase Realtime, or another host-authoritative state machine before larger live games. |
| P1 | Server structure | `server/app.ts` is still a large multi-domain route file. Quiz state and Annual Conference use-case/storage logic have been extracted, but auth, health, feedback, attendance, event CRUD, quiz routes, user merge, and public APIs still share one route module. | `server/app.ts`; `server/quiz-state.ts`; `server/annual-conference-service.ts` | Mount additional domain route modules and introduce shared request validation/error helpers; Annual Conference routes can move next without moving their application logic again. |
| P1 | Test coverage | Coverage now includes mock DB hardening, Luma attendance parsing, and quiz state helpers, but active Hono route integration, Vue views, feedback flows, and Supabase helpers still have little or no direct automated coverage. | `lib/mock-db/index.test.ts`; `lib/luma-attendance.test.ts`; `server/quiz-state.test.ts` | Add route-level tests for Hono handlers, component tests for key forms, and integration smoke tests for public API. |
| P1 | Admin auth hardening | Hosted admin auth has per-email Supabase login, distributed token-exchange limits, hashed app sessions, owner/organizer/volunteer roles, live role checks, immediate revocation, and audit rows, but the app cannot enforce MFA on the upstream Google account. | `lib/supabase/admin-auth.ts`; `server/app.ts`; `supabase/migrations/20260616000000_admin_auth.sql`; `docs/auth.md` | Require MFA for owner Google accounts, test risky-sign-in alert delivery, and consider Cloudflare Access as an outer privileged-access gate. |
| P1 | Security monitoring | Structured request, authentication, provider, rate-limit, and mutation-audit events exist, but Cloudflare, Supabase, and Google alert rules and retention are external to this repository and have not been verified in this audit. | `server/app.ts`; `lib/supabase/admin-auth.ts`; `docs/security-audit-2026-08-03.md` | Configure and exercise alerts for elevated `401`, `403`, `413`, `429`, `5xx`, risky sign-ins, role changes, and security-dependency failures. |
| P1 | Deployment assurance | A repeatable read-only production verifier and production-denied staging DAST runner now exist. Worker version `f3b487a8-3971-4cb2-8bde-a1a3b1cadd30` resolved private caching, API cross-domain policy, and credentialed localhost CORS; the canonical origin now passes 28/31 checks, with three Pages-layer assertions pending the reviewed Pages release. No isolated staging hostname/bindings are configured, and the available Cloudflare token cannot enumerate Pages deployments. | `scripts/security/*`; `docs/deployed-security-verification-2026-08-03.md`; `public/_worker.js`; `wrangler.toml` | Release the reviewed Pages build, grant deployment-read visibility, provision isolated staging, then run passive, active, and authenticated authorization checks. |
| P1 | Secrets posture | Sentinel finds five real credentials only in ignored mode-0600 `.env.local` plus the expected public Supabase anonymous key in ignored browser output. The current 154-commit Gitleaks scan found no history leak, and exact comparison found no service-role/provider key in `dist/`. | `.sentinel/security.md`; `.sentinel/secrets-found.md`; `SECURITY.md` | Keep local credentials machine-only and rotate any value ever copied into chat, logs, issues, screenshots, or another untrusted system. |
| P2 | Deployment readiness | The target Cloudflare/Supabase architecture is documented, but runtime still assumes Bun/static serving and JSON file behavior in places. | `docs/deployment-cloudflare-supabase.md`; `server/index.ts`; `lib/mock-db/*` | Complete the Supabase migration and decide whether production runs as Cloudflare Workers/Pages, Render bridge, or another hosted Bun target. |
| P2 | Mobile/performance follow-up | Phone route canonicalization, a deliberately limited Mobile Ops surface, tablet navigation at 768–1023px, stable small-viewport units, and initial-bundle splitting have been verified in an emulated browser. The earlier reported intermittent black header mark was not reproduced. Real iOS/Android checks and a Chrome DevTools Core Web Vitals trace remain outstanding. | `src/organizer-viewport.ts`; `src/router.ts`; `src/App.vue`; `src/styles.css`; `src/views/admin/AdminMobileOrganizerView.vue` | Run real-device Safari/Android smoke tests and a recorded Chrome DevTools trace when that integration is available. Keep phone design judgment separate from objective platform defects, and retain tablet/desktop feature parity at widths of 768px and above. |
| P2 | Input validation | Several API handlers parse request JSON inline and validate manually, which makes behavior inconsistent across domains. | `server/app.ts` route handlers | Introduce typed request schemas and shared validation response helpers. |
| P2 | Code review/audit checklist | Recent organizer work spans auth, mobile routing, performance, audit log density, feedback, attendance, and Talk Management. There is not yet a standing review checklist for cross-cutting regressions such as stale deployed bundles, route guards, local-vs-Supabase source mode, keyboard/mobile viewport behavior, and audit-log coverage. | `src/App.vue`; `src/router.ts`; `lib/supabase/server.ts`; `server/app.ts`; `src/views/admin/*` | Before the next PR/release, run a structured code review and audit pass covering auth/session mode, data-source mode, mobile navigation, route performance, accessibility, and audit entries for organizer mutations. For mobile findings, use the `$thumb-first` unified report format so design forks and objective platform defects stay visually distinct. |
| P2 | Admin client data layer | Most admin event sub-views fetch with raw `fetch` in `onMounted`, bypassing the vue-query cache; `GET /api/events/:id` fires twice per mount (view + `AdminEventTabs` query), sub-page navigation refetches everything, and mutations keep local copies instead of writing server responses into the cache. | `src/views/admin/AdminEventView.vue`; `AdminTalksView.vue`; `AdminSpeakersView.vue`; `AdminAttendanceView.vue`; `AdminFeedbackView.vue`; `AdminQuizView.vue`; `src/components/AdminEventTabs.vue` | Migrate view reads to `useQuery` with the shared `queryKeys`, and write mutation responses into the cache with `queryClient.setQueryData` (pattern already used in `AdminFeedbackOverviewView.vue`). |
| P2 | Quiz auth boundary | Public join, answer, personal state, and participant-name routes are explicitly admitted, device-owned, body-bounded, and rate-limited where they mutate. Legacy `/api/quiz/active`, `/api/leaderboard`, and state advancement remain organizer-protected, so the general quiz flow is still paused while System Design rooms use the hardened participant subset. | `server/app.ts` (`isUnauthenticatedApiRequest`, quiz routes); `server/security-boundaries.test.ts`; `server/system-design-participant-name.test.ts` | Before resuming general quizzes, define the public read/advance contract and replace the browser device ID with a scoped, expiring server-issued participant token if participant identity gains higher value. |
| P2 | Feedback monthly N+1 | `GET /api/feedback/monthly` issues 2-3 store queries per event (campaign + questions + submissions) instead of one bulk fetch grouped in memory. | `server/app.ts` (`/api/feedback/monthly`); `lib/supabase/feedback-campaigns.ts` | Add bulk campaign/submission fetches keyed by event id and group in memory, or wait for the relational migration wave. |
| P2 | Reads with side effects | Several GET handlers write: get-or-create feedback campaigns on admin and public reads, checklist template backfill/status catch-up on checklist reads, and remote backfill-on-read in the mock DB document layer. | `server/app.ts` (`/api/events/:id/feedback-campaign`, `/api/feedback/events/:id*`); `lib/mock-db/event-checklists.ts`; `lib/mock-db/index.ts` (`readRemoteData`) | Create defaults at event-creation time and make GET handlers read-only (return computed defaults without persisting). |
| P2 | Unbounded payloads | Admin-polled endpoints return full collections without limits: overview, leaderboard, talks, quiz sessions, feedback submissions per event, and the attendance monthly ledger with embedded raw CSV records. | `server/app.ts` (`/api/overview`, `/api/leaderboard`, `/api/talks`, `/api/quiz/sessions`, `/api/attendance/monthly`) | Add `limit`/cursor params, cap leaderboards (top 50/100), and return attendance records on demand instead of inside the ledger payload. |
| P3 | Date formatting in rows | Mobile Ops now reuses a hoisted formatter, but several other admin views still construct fresh `Intl.DateTimeFormat`/`toLocaleString` formatters per row per render (audit log, attendance, talks, feedback, organizers). | `src/views/admin/*` (e.g. `AdminAuditLogView.vue`, `AdminAttendanceView.vue`, `AdminEventView.vue`) | Hoist shared module-level formatters into a small date utility and reuse `.format()`. |
| P3 | Legacy migration cleanup | Legacy Next/React files remain as reference while active work happens in Vue/Hono. | `app/`, `components/`, `hooks/`; `docs/migration-parity.md` | Remove or archive legacy areas once Vue parity is complete and no docs still depend on them. |

## Good First Debt Issues

- Add Hono route integration tests around quiz state, attendance import, and feedback routes.
- Extract one small route group from `server/app.ts` into a domain route module.
- Add schema validation for one API endpoint group.
- Add a Playwright mobile/header regression script for the `$thumb-first-platform` pass that reloads organizer routes, switches Organizer/Community, screenshots the header, and logs the DOM element under any unexpected black mark.
- Draft a PR review checklist for organizer auth, local JSON vs Supabase mode, mobile route behavior, performance budget, and audit-log coverage, with `$thumb-first-design` and `$thumb-first-platform` sections for mobile work.
- Improve README/CONTRIBUTING guidance around generated reports once the repo policy is decided.
- Add a GitHub Actions workflow for `pnpm typecheck`, `pnpm test`, and `pnpm build`, then add a real CI badge.

## Review Cadence

Review this file before major releases, deployment work, or onboarding contributor issues. Move items out when they are fixed, and add a changelog entry for meaningful debt reduction.
